rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /orders/{orderId} {
      // Allow creation and reading for anyone (for testing purposes) - revert this for production!
      allow read, create: if true;
      // Allow updating for authenticated users
      allow update: if request.auth != null;
    }
  }
}
